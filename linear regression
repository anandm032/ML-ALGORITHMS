import pandas as pd
df=pd.read_csv("C:/Users/anand/Desktop/housing_corrected.csv")
"""
print(df.tail())
print(df.columns)
print(df.shape)

print(df.isnull())
print(df.corr()["medv"])
"""

x=pd.DataFrame(df["lstat"])
y=pd.DataFrame(df["medv"])

from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=0.2,random_state=20)

from sklearn.linear_model import LinearRegression
lreg=LinearRegression()
lreg.fit(x_train,y_train)
y_pred = lreg.predict(x_test)
y_pred = pd.DataFrame(y_pred,columns=["predicted"])
df_pred = pd.DataFrame(columns=["Actual","pred"])
df_pred["Actual"] = y_test.reset_index(drop=True)["medv"]
df_pred["pred"] = y_pred.reset_index(drop=True)["predicted"]
print(df_pred)

from sklearn.metrics import mean_squared_error, r2_score

print("MSE:", mean_squared_error(y_test, y_pred))
print("R2 Score:", r2_score(y_test, y_pred))
